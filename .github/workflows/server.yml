name: Server workflow
run-name: ${{ github.actor }} has made a pull request 🚀

on:
  pull_request:
    branches:
      - develop
      - staging
      - main
env:
  DATABASE_URL=postgresql://rabble:CLmvJ_n2kTtnAinLtpKM_A@forked-bear-4276.6zw.cockroachlabs.cloud:26257/defaultdb?sslmode=verify-full
  PORT=3000
  TWILO_SID=AC1a9ec537ffce149471c5fd999b7ba779
  TWILO_AUTH_TOKEN=4a306534475082f57ce51b01032b2f32
  TWILO_PHONE=+14406933330
  JWT_SECRET=rab4a306534475082f57ce51b01032b2f32ble
      
jobs:
  rabble-backend:
     runs-on: ubuntu-latest
     strategy:
      matrix:
       node-version: [16.x]  
       
     steps:
      - name: Checkout Source 📄
        uses: actions/checkout@v3     

      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v1
        with:
          node-version: ${{ matrix.node-version }}

      - name: Install dependencies 📦️
        run: npm ci

      - name: Lint 🧐
        run: npm run lint

      - name: Build 🏗️
        run: npm run build

      - name: Run tests (unit) 🧪
        run: npm test

      - name: Run tests (e2e) ⚙️
        run: npm run test:e2e


